# Server Configuration
server.port=${SERVER_PORT:8080}

# Application Name
spring.application.name=mainservice

# Database Configuration - Uses environment variables (set in Azure), falls back to local for dev
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/smartcare?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:venith123}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=false

# JWT Configuration
jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
jwt.expiration=86400000

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost:5173,http://localhost:3000,https://frontend.mangobush-8de88b36.southeastasia.azurecontainerapps.io
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*

# Default Admin Account Configuration
admin.default.username=admin
admin.default.password=admin123
admin.default.email=admin@hospital.com
admin.default.name=System Administrator

# Frontend URL Configuration
app.frontend.url=https://frontend.mangobush-8de88b36.southeastasia.azurecontainerapps.io

# Mail configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:smarthealthcaremonitor@gmail.com}
spring.mail.password=${MAIL_PASSWORD:bugucvcwpmgtvsec}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# FILE UPLOAD CONFIGURATION
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
spring.servlet.multipart.enabled=true
spring.servlet.multipart.location=${java.io.tmpdir}
spring.servlet.multipart.file-size-threshold=2KB

# WEBSOCKET CONFIGURATION
spring.websocket.servlet.allowed-origins=*
logging.level.org.springframework.web.socket=INFO
logging.level.org.springframework.messaging=INFO

# PayHere
payhere.merchantId=1234488
payhere.merchantSecret=MTE4MTY2MjI1NDI4MDA2OTAzMTYxNzE0ODkyMzkxMzM2MzE5NDg0
payhere.returnUrl=https://frontend.mangobush-8de88b36.southeastasia.azurecontainerapps.io/payment-success
payhere.cancelUrl=https://frontend.mangobush-8de88b36.southeastasia.azurecontainerapps.io/payment-cancel
payhere.notifyUrl=https://torchlike-stanley-worryingly.ngrok-free.dev/api/payments/notify

# OCR - disabled on Azure (no Tesseract installed)
ocr.tesseract.datapath=/usr/share/tesseract-ocr/4.00/tessdata
ocr.tesseract.lang=eng
ocr.maxChars=8000

vitalreports.url=${VITALREPORTS_URL:http://localhost:8081}

# Kafka - uses environment variable (set in Azure)
#spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:kafka:9092}
#spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
#spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer
# Kafka - DISABLED (not used yet)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

#kafka.topic.sensor-data=${SENSOR_TOPIC:sensor-data-topic}

# Eureka registration - uses environment variable (set in Azure)
eureka.client.service-url.defaultZone=${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka}

# Actuator + Prometheus
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.health.show-details=always
management.metrics.tags.application=${spring.application.name}

# Debug logging to catch 500 errors
logging.level.com.example.mainservice=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
